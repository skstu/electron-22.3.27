import { ParsedDocumentationResult } from './ParsedDocumentation';
export declare class DocsParser {
    private baseElectronDir;
    private moduleVersion;
    private apiFiles;
    private structureFiles;
    private packageMode;
    constructor(baseElectronDir: string, moduleVersion: string, apiFiles: string[], structureFiles: string[], packageMode: 'single' | 'multi');
    private parseBaseContainers;
    private parseAPIFile;
    private parseStructure;
    parse(): Promise<ParsedDocumentationResult>;
}
